import { motion } from 'framer-motion';
export default function DesignPhilosophy() { const items=[['Requirements first','Define targets, constraints, interfaces, and failure criteria before optimizing components.'],['Analysis before claims','Use CAD, FEA, kinematics, dynamics, and data to support decisions rather than decorate them.'],['Test to learn','Physical testing closes the loop between the model and the machine.'],['Iterate deliberately','Every revision should have a reason, an expected effect, and evidence behind it.']]; return <section className="py-24 bg-black"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="max-w-3xl"><p className="text-red-500 font-mono text-sm">DESIGN PHILOSOPHY</p><h2 className="mt-3 text-4xl font-bold text-white">Engineering is a process, not a claim.</h2><p className="mt-5 text-gray-400 text-lg">The goal is not to make the vehicle look engineered. The goal is to make the engineering traceable.</p></div><div className="grid md:grid-cols-2 gap-6 mt-12">{items.map(([t,d],i)=><motion.div key={t} initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.08}} className="border border-white/10 bg-gray-950 p-7"><div className="text-red-600 font-mono">0{i+1}</div><h3 className="mt-4 text-xl font-semibold text-white">{t}</h3><p className="mt-3 text-gray-400">{d}</p></motion.div>)}</div></div></section>; }
